{
  "name": "easyloop",
  "version": "1.3.0",
  "description": "EasyLoop - AMR Mapping, Loop Closure, Control and Diagnostics",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:win": "pkg --config package.json launcher.js --targets node18-win-x64 --output dist/EasyLoop.exe --compress GZip",
    "build:linux": "pkg --config package.json launcher.js --targets node18-linux-x64 --output dist/easyloop --compress GZip",
    "build:all": "pkg --config package.json launcher.js --targets node18-win-x64,node18-linux-x64 --output dist/easyloop --compress GZip",
    "lint": "eslint .",
    "test": "jest"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "ssh2": "^1.15.0",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "pkg": "5.8.1"
  },
  "author": "",
  "license": "MIT",
  "pkg": {
    "scripts": ["server.js", "server/**/*.js"],
    "assets": [
      "public/**/*",
      "config/**/*",
      "server/**/*",
      ".env"
    ],
    "outputPath": "dist"
  }
}
